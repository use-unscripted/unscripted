import { useState } from 'react'; import { useNavigate } from 'react-router-dom'; import { base44 } from '@/api/base44Client'; import Field from '@/components/onboarding/Field'; import { Clock3, ArrowRight } from 'lucide-react';
const fields=[['class_blocks','Class schedule','Mon/Wed 10–12, Tue/Thu 2–4...'],['work_blocks','Work schedule','Shifts or recurring work'],['club_blocks','Club commitments','Meetings and events'],['athletic_blocks','Athletic commitments','Practice, training, games'],['study_blocks','Study blocks','When you already plan to study'],['social_commitments','Social commitments','Recurring plans worth protecting'],['preferred_deep_work_times','Best deep-work times','When are you sharpest?'],['low_energy_times','Usually low-energy','When should we avoid demanding work?']];
export default function ScheduleInput(){const nav=useNavigate();const [data,setData]=useState({available_hours_per_week:8});const [saving,setSaving]=useState(false);const change=e=>setData({...data,[e.target.name]:e.target.type==='number'?Number(e.target.value):e.target.value});const submit=async()=>{setSaving(true);await base44.entities.Schedule.create(data);nav('/generating')};return <main className="min-h-screen bg-[#f4f7fb] px-5 py-10"><div className="mx-auto max-w-4xl"><div className="mb-8"><div className="mb-4 flex h-11 w-11 items-center justify-center rounded-xl bg-blue-600 text-white"><Clock3/></div><h1 className="text-4xl font-bold tracking-tight text-[#07152f]">Build around your real week.</h1><p className="mt-3 text-slate-500">Your roadmap should fit your life—not compete with it.</p></div><section className="grid gap-5 rounded-3xl border bg-white p-7 sm:grid-cols-2 sm:p-10">{fields.map(([name,label,ph])=><Field key={name} name={name} label={label} placeholder={ph} value={data[name]} onChange={change}/>)}<div className="sm:col-span-2"><Field type="number" name="available_hours_per_week" label="Realistic weekly hours for growth" value={data.available_hours_per_week} onChange={change}/><p className="mt-2 text-xs text-slate-400">Be honest. A focused 6 hours beats an imaginary 20.</p></div><button onClick={submit} disabled={saving} className="mt-4 flex items-center justify-center gap-2 rounded-xl bg-blue-600 px-6 py-3.5 font-bold text-white sm:col-span-2">{saving?'Saving your week...':'Generate my Ambition Profile'} <ArrowRight size={18}/></button></section></div></main>}