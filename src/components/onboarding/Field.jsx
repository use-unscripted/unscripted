export default function Field({label,name,value,onChange,placeholder,type='text',rows}){const cls='mt-2 w-full rounded-xl border border-slate-200 bg-white px-4 py-3 text-sm outline-none transition focus:border-blue-500 focus:ring-4 focus:ring-blue-500/10';return <label className="block text-sm font-semibold text-slate-700">{label}{rows?<textarea rows={rows} name={name} value={value||''} onChange={onChange} placeholder={placeholder} className={cls}/>:<input type={type} name={name} value={value||''} onChange={onChange} placeholder={placeholder} className={cls}/>}</label>}